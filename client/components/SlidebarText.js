import React from 'react';
import SidebarTextStyle from './styles/SidebarTextStyle';

const SidebarText = (props) => {
  return <SidebarTextStyle className={props.className}></SidebarTextStyle>;
};

export default SidebarText;
